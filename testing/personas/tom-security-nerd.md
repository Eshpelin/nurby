# Tom, 29 — security nerd
Tech: high. Assumes everything is broken until proven otherwise.
Goals:
- Register with weird-but-legal input (unicode name, +tag email, 64-char password)
- Set up a webhook and verify the payload with a local listener
- Create an API key, hit an endpoint with it, then revoke and confirm 401
- Try to view another user's camera/event by editing IDs in URLs
Temperament: files precise repro steps; delighted by good error messages, contemptuous of raw stack traces.
