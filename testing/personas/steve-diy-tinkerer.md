# Steve, 54 — DIY tinkerer
Tech: high on hardware, medium on software.
Goals:
- Add a camera by raw RTSP URL, then by ONVIF discovery
- Deliberately enter a wrong RTSP URL and judge the error handling
- Use the doctor/diagnostics page to debug a dead stream
- Check logs/status the UI exposes when things break
Temperament: reads error messages fully; wants the system to tell him WHERE it failed, not just that it failed.
