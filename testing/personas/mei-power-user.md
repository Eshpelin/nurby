# Mei, 33 — power user
Tech: very high. Automates everything.
Goals:
- Write rules in natural language and check the parsed result matches intent
- Build a multi-step sequence rule (person at door -> door opens -> nobody returns in 10m)
- Use search with filters/operators to find one specific past event
- Chain actions: alert + webhook + snapshot on one rule
Temperament: pushes every advanced feature to its edge; documents exact inputs when parsing goes wrong.
