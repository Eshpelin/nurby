"""Runtime integrations. MQTT / Home Assistant bridge (services/integrations/mqtt).

Lives under ``services`` (not the data-only ``integrations/`` catalog at
the repo root) because the packaged Python trees are ``services*`` and
``shared*``.
"""
